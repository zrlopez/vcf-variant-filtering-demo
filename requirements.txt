# ============================================================
# requirements.txt — vcf-variant-filtering-demo
# Last updated: 2026-05-22
# Python: >=3.11
# All packages pinned to security-reviewed releases.
# Run `pip-audit` before every production deployment.
# ============================================================

# ── Core data processing ──────────────────────────────────
pandas==2.2.2
numpy==1.26.4
scipy==1.13.1
scikit-learn==1.5.0

# ── Visualization ─────────────────────────────────────────
matplotlib==3.9.0
seaborn==0.13.2
plotly==5.22.0

# ── Genomics ──────────────────────────────────────────────
pysam==0.22.1
cyvcf2==0.30.22
pybedtools==0.10.0

# ── CLI ───────────────────────────────────────────────────
click==8.1.7
tqdm==4.66.4
colorama==0.4.6

# ── Templating (autoescape enforced in code) ──────────────
jinja2>=3.1.6       # Dependabot GHSA sandbox breakouts

# ── Jupyter (non-default; install only for local notebooks) ─
# Pinned to patched versions. DO NOT install in production.
# jupyter==4.2.5    # Uncomment for local notebook use only
# notebook==7.2.2   # CVE-2023-40596 patched in 6.5.6+

# ── Development / testing (install via: pip install -e .[dev]) ─
# Listed here for CI compatibility — keep in sync with pyproject.toml [dev]
pytest>=9.0.3
black>=26.3.1
flake8==7.1.0
mypy==1.10.0

# ── Documentation ─────────────────────────────────────────
sphinx==7.4.7
nbconvert>=7.17.1
